# OSLab-homeworks
1. you can also use ‘-eq’ for equality, ‘-ne’ for not equality and ‘-gt’ for greater than and ‘-lt’ for less than in bash script.
   you can also use ‘&&’ for and , ‘||’ for or in bash script.
   for example : if [[ ( $username == "admin" && $password == "secret" ) ]]; then ...
2. make directory user1 to user100
3. rename file.jpg and file.png to img1,img2,...
   mv [option] source destination